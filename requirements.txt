# Pins exatos, nas versões que produção roda hoje. A faixa saiu quando o job
# `lock` do CI entrou: ele exige `==` em toda linha, porque só assim o
# requirements.lock (com os hashes de tudo, inclusive transitivas) pode ser
# conferido contra este arquivo.
#
# Para atualizar: suba o pin aqui e regere o lock com o script do CI central —
#   python CLONE_DO_CI/scripts/gerar_lock.py --python-version 3.12
#
# (o script mora no rigst/ci e não é versionado aqui; o cabeçalho do
# requirements.lock repete a invocação exata usada)
# O Dependabot bump a linha daqui, mas não conhece o .lock; o job `lock` é
# quem acusa a divergência entre os dois.
Django==6.0.8
celery==5.6.3
redis==8.1.0
python-dotenv==1.2.2
# Extração de PDF
pdfplumber==0.11.10
pymupdf==1.28.2
pillow==12.3.0
# IA
anthropic==0.121.0
# Relatórios
sentry-sdk==2.68.1
weasyprint==69.0
markdown==3.10.3
# Produção
gunicorn==26.0.0
psycopg2-binary==2.9.12
django-redis==6.0.0
pyspellchecker==0.9.0

# Conformidade legal (app `legal`): render e sanitização das políticas.
nh3==0.3.6

# Tema do admin.
django-unfold==0.104.0

# O pip entra aqui como qualquer outra dependência, e não como ferramenta à
# parte. Ele é o que o CD executa a cada deploy, e enquanto não estava
# declarado envelhecia sem que nada avisasse: em 02/09/2026 uma auditoria dos
# venvs de produção achou o pip como o ÚNICO pacote vulnerável do parque (um
# deles com a 24.0 e 7 achados), justamente porque nenhum PR do Dependabot o
# cobria. Declarado, ele cai na mesma máquina do resto — Dependabot propõe,
# lock guarda o hash, CD instala.
#
# Não use `pip install --upgrade pip` no deploy: isso instalaria versão não
# fixada, que é o que o pin e o `--require-hashes` existem para evitar.
pip==26.2.1
